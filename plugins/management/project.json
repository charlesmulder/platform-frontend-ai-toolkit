{
  "name": "management-plugin",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "plugins/management",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:noop",
      "outputs": []
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd plugins/management/skills/jira-issue-creator && python3 -m venv .venv && .venv/bin/pip install -q -r requirements.txt && .venv/bin/pytest tests/ -v",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
